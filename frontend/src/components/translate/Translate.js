import React from 'react';

const Translate = () => {
  
  return (
    <canvas id="background-canvas" className="fixed inset-0 w-full h-full pointer-events-none">
    </canvas>
  )
};

export default Translate;
